Now I'll build a comprehensive, production-grade EKS Cost Optimization tool with your corporate Gen AI Studio token support.

Here's your **EKS Cost Intelligence** tool — a production-grade platform engineering artifact. Here's what it does:

---

**Three-tab workflow:**

1. **⚙ Config** — Paste your corporate Gen AI Studio token, set the base URL (your enterprise proxy endpoint), and select model. The token is sent as `x-api-key` directly to your endpoint — nothing stored.

2. **📡 Cluster** — Paste your EKS cluster JSON (node groups, workloads, addons, networking, storage, observability). A realistic sample is preloaded. Select **focus areas** (compute, spot, reservations, autoscaling, storage, networking, observability, workload right-sizing, or all).

3. **📊 Analysis** — The AI returns a structured report with:
   - **Executive summary** — current cost, potential savings %, annual savings estimate, health score
   - **Cost breakdown** — bar chart by category with optimization status
   - **Quick wins** — zero-risk actions executable today
   - **Prioritized recommendations** — expandable cards with action steps, risks, effort level, and dollar estimates
   - **Architecture insights** — narrative from the AI
   - **JSON export** button for sharing with your team

---

**To use with your corporate endpoint**, set the base URL to your Gen AI Studio proxy (e.g. `https://genai.corp.internal`) and paste your enterprise token. The app calls `/v1/messages` against it directly from your browser.