
Act as a peincipal platfoem engineer to build a comprehensive ai powered tool to analyse and possible suggest cost savings on eks cluster. 

Consider using my corporate gen ai studio api auth token

For Cluster configs, dont want to paste configs (node groups, workloads, addons, networking, storage, observability) manually, but rather just pass the github repo and path for cluster config then the tool clone the config and does the analyse.


Now I'll build the full enhanced version with GitHub integration — recursive file tree browsing, YAML parsing, and a two-stage AI pipeline (normalize → analyze).

Here's what changed and how it works now:

---

**GitHub-native ingestion — no more manual pasting:**

**📡 Repository tab** — just fill in:
- **Repo URL** — `https://github.com/myorg/eks-platform`
- **Branch** — `main`, `staging`, etc.
- **Path** — optional subdirectory like `clusters/prod` or `infra/eks`
- **GitHub PAT** — only needed for private repos; public repos work without one

Click **Scan Repository** → the tool hits the GitHub Trees API (single request, recursive) and builds an interactive file tree showing every YAML, Terraform, Helm values, and Kustomize file it finds.

**File tree browser** — checkboxes let you include/exclude individual files before analysis. Irrelevant files (`.md`, `.png`, etc.) are greyed out automatically. Relevant config files are pre-selected.

---

**Two-stage AI pipeline:**

| Stage | What happens |
|---|---|
| **1 – Fetch** | Fetches each selected file's raw content from GitHub blobs API |
| **2 – Normalize** | Claude reads raw eksctl YAML / Terraform HCL / Helm values / k8s manifests and extracts a structured cluster cost profile — no manual formatting needed |
| **3 – Analyze** | Claude runs the full cost optimization analysis on the normalized profile |

The normalized cluster profile is shown as a collapsible preview between stages, so you can verify what Claude extracted before seeing the recommendations.